import os.path
import time
from math import *
from math import sqrt

def insert_ph(time_int, number):
	for i in range(int(number)):
		os.system("qsub sh")
		time.sleep(30)
	return 0

insert_ph(1, 12)
